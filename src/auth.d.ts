declare module "#auth-utils" {
  interface User {
    pid: string;
    first_name: string;
    last_name: string;
    email: string;
  }

  // interface UserSession { }

  // interface SecureSessionData { }
}

export {};
